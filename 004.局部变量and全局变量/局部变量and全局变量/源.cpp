#include <stdio.h>
int num2 = 20;                           //全局变量-定义在代码块（{}）之外的变量
int main()
{
	int num1 = 10;                       //局部变量-定义在代码块（{}）内部
	int num2 = 100;
	printf("%d\n", num2);
	//{
	//	int num3 = 20;
	//}
	//printf("%d\n", num3);        //无法在{}的{}中创建局部变量，会出现报错       
	return 0;
}

//局部变量和全局变量的名字建议不要相同-容易误会，产生bug
//当局部变量和全局变量的名字相同的时候，局部变量优先